# Puerto del servidor admin (distinto al backend público)
PORT=5001

# Misma URI de MongoDB Atlas que el backend público
MONGO_URI=mongodb+srv://exploreadmin:exploreadmin@exploreoccidente.sftskgn.mongodb.net/explorewebpage?retryWrites=true&w=majority&appName=ExploreOccidente

# Secreto para firmar los JWT — cambiá esto por algo seguro
JWT_SECRET=cambia_esto_por_algo_muy_seguro_123

# Credenciales del administrador
ADMIN_EMAIL=admin@exploreoccidente.cr
ADMIN_PASSWORD=cambia_esto

# URL del frontend admin (para CORS)
ADMIN_FRONTEND_URL=http://localhost:5174
